# ad_text_gpt
This is a GPT which is creating add text for Snocks. 

## GPT app configuration

This is the configuration we have in the GPT app:

![GPT app configuration screenshot](docs/gpt-app-config.png)

> Note: place the screenshot file at `docs/gpt-app-config.png` so it renders here.
